#!/usr/bin/env bash

# Copy custom config files. Home Assistant requires configuration to be editable runtime, but we want configuration as
# code.
for config in /maison/conf/*.yaml; do
    cp "$config" /config/
done

# Invoke the Docker image's original command.
python -m homeassistant --config /config
